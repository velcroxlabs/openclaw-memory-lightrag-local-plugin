{
  "id": "memory-lightrag-local",
  "name": "Memory (LightRAG Local)",
  "description": "OpenClaw plugin for LightRAG local memory backend",
  "version": "0.2.0",
  "kind": "memory",
  "uiHints": {
    "baseUrl": {
      "label": "LightRAG API Base URL",
      "placeholder": "http://127.0.0.1:8787"
    },
    "apiKey": {
      "label": "API Key",
      "sensitive": true,
      "placeholder": "..."
    },
    "autoIngest": {
      "label": "Auto-Ingest",
      "help": "Capture conversation content from hooks and ingest into adapter"
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Inject relevant memories before each AI turn"
    },
    "maxRecallResults": {
      "label": "Max Recall Results",
      "placeholder": "8",
      "help": "Maximum memory snippets injected per turn",
      "advanced": true
    },
    "captureMode": {
      "label": "Capture Mode",
      "help": "all (default): remove injected context blocks. everything: ingest as-is",
      "advanced": true
    },
    "minCaptureLength": {
      "label": "Min Capture Length",
      "placeholder": "10",
      "help": "Skip captured text shorter than this",
      "advanced": true
    },
    "debug": {
      "label": "Debug Logging",
      "help": "Enable verbose plugin diagnostics",
      "advanced": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Base URL of the LightRAG local adapter API (e.g., http://127.0.0.1:8787)"
      },
      "apiKey": {
        "type": "string",
        "description": "API key for authenticating with the LightRAG adapter"
      },
      "autoIngest": {
        "type": "boolean",
        "description": "Whether to automatically capture conversation content and ingest into adapter",
        "default": true
      },
      "autoRecall": {
        "type": "boolean",
        "description": "Whether to automatically inject relevant memories before each AI turn",
        "default": true
      },
      "maxRecallResults": {
        "type": "number",
        "minimum": 1,
        "maximum": 20,
        "description": "Maximum number of memory snippets to inject per turn",
        "default": 8
      },
      "captureMode": {
        "type": "string",
        "enum": ["all", "everything"],
        "description": "all: remove injected context blocks; everything: ingest as-is",
        "default": "all"
      },
      "minCaptureLength": {
        "type": "number",
        "minimum": 1,
        "maximum": 200,
        "description": "Skip captured text shorter than this length",
        "default": 10
      },
      "debug": {
        "type": "boolean",
        "description": "Enable verbose plugin diagnostics",
        "default": false
      }
    },
    "required": ["baseUrl", "apiKey"]
  }
}
